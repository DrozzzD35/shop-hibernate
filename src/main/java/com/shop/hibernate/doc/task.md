## 📝 Задание

### Основное задание

Создай простое приложение с Hibernate:

1. **Entity классы:**
    - `Category` (id, name, description)
    - `Product` (id, name, price, category)

2. **Repository с EntityManager:**
    - CRUD операции
    - Поиск продуктов по категории
    - Поиск продуктов дороже X

3. **Service с @Transactional:**
    - Создание категории с продуктами
    - Получение всех продуктов категории

4. **Тест:**
    - H2 in-memory
    - Проверка всех операций

**Критерии:**
- [ ] Entity корректно маппятся на таблицы
- [ ] CRUD работает
- [ ] Транзакции используются правильно
- [ ] show-sql показывает SQL

### ⭐ Задание со звёздочкой

Добавь:
- Аудит: createdAt, updatedAt с @PrePersist, @PreUpdate
- Soft delete: поле deletedAt вместо реального удаления
- Версионирование: @Version для optimistic locking

---